






















Ниже представлено полное и максимально детальное техническое описание логики твоей стратегии. Это «библия» скрипта, по которой можно воссоздать его с нуля без единой ошибки.

---

### 1. ОБЩИЕ ПАРАМЕТРЫ И ИНДИКАТОРЫ

* **Независимость позиций:** Параметр `pyramiding=2`. Лонг и Шорт работают как две разные стратегии: могут быть открыты одновременно, иметь свои стопы и причины выхода.
* **Кнопка «Переворот» (Flip):**
* **ВКЛ:** Если при открытом Лонге приходит сигнал в Шорт — сначала закрываем Лонг, затем открываем Шорт.
* **ВЫКЛ:** Позиции открываются независимо (может быть Long + Short одновременно).


* **Индикаторы (Жестко заданные константы):**
* **RSI:** Период 14.
* **VFI:** Длина 130, Коэффициент 0.2, Макс. объем 2.5.
* **ADX/DI+:** Период 14 (используется только DI+ для фильтров).


* **Пивоты:** Используются `ta.pivothigh(2, 2)` и `ta.pivotlow(2, 2)`. При нахождении нового пивота он сравнивается с предыдущим того же типа для определения статуса: **HH** (выше прошлого), **LH** (ниже прошлого), **HL** (минимум выше прошлого), **LL** (минимум ниже прошлого).

---

### 2. ЛОГИКА ВХОДА (ЛОНГ)

#### А. Свечной паттерн

Скрипт ищет комбинацию: **минимум 3 красные свечи подряд → 1 зеленая свеча**.

* *Зеленая свеча* — это «сигнальный бар». Цена входа (в будущем) будет ценой закрытия бара, на котором сработает логика.

#### Б. Поиск условий индикаторов (Блоки LB1–LB16)

Условия из выбранного блока (например, LB1: RSI < 30 + VFI > 10) ищутся **на всем отрезке паттерна** (все красные свечи + сигнальная зеленая).

* **Пример:** Было 4 красных и 1 зеленая. Если RSI был < 30 на 2-й красной, а VFI стал > 10 на финальной зеленой — условие блока выполнено.

#### В. Пивот-фильтр входа (4 варианта паттерна)

Если условия индикаторов выполнены, скрипт проверяет включенные пивот-варианты (например, LL → LH):

* **Поиск без шума:** Скрипт ищет первый пивот (LL) в окне `pLE_WinBack`. Найдя его, ищет второй (LH) **строго после него** в пределах общего окна (`WinBack + WinFwd`). Любые другие пивоты между ними игнорируются.
* **Сценарий 1 (Пивот в прошлом):** Оба пивота уже есть на момент закрытия зеленой свечи.
* *Действие:* Вход в Лонг сразу на закрытии зеленой свечи.


* **Сценарий 2 (Пивот в будущем):** Индикаторы сработали, но второй пивот еще не появился. Скрипт ждет его появления в «Окне вперед».
* *Действие:* Если нужный пивот (LH) сформировался через 3 свечи после зеленой — вход происходит по цене закрытия этой 3-й свечи.



---

### 3. ЛОГИКА ВХОДА (ШОРТ) — Зеркально

* **Паттерн:** 3+ зеленые свечи → 1 красная.
* **Индикаторы:** Блоки SB1–SB16 проверяются на всех свечах паттерна (зеленые + красная).
* **Пивоты:** Ищет паттерны (например, HH → LH). Если пивот найден позже (в окне вперед), вход происходит на закрытии свечи этого пивота.

---

### 4. ЛОГИКА ВЫХОДА (ЛОНГ)

#### А. Прямая продажа (Блоки LSD1–LSD3)

Это моментальный выход без фильтров.

* **Условие:** Если на текущей свече (и только на ней, без окна поиска) RSI, VFI или DI+ коснулись заданного уровня.
* **Действие:** Моментальная активация трейлинг-стопа. Начальный уровень стопа — `low` предыдущей свечи (`low[1]`).

#### Б. Основная продажа (Блоки LS1–LS16) — Каскадная система

* **Каскад:** Скрипт ищет выполнение всех условий блока. Если в блоке 2 условия (RSI и VFI), он находит одно, и в радиусе `± signalWindowLong` ищет второе.
* **Бар Сигнала:** Тот бар, на котором хронологически последним выполнилось условие индикатора.

#### В. Пивот-фильтр выхода и постановка стопа

Если включен пивот-фильтр (например, HH → LH):

1. Скрипт ищет пару пивотов (игнорируя шум) в радиусе `WinBack/WinFwd` от **Бара Сигнала**.
2. **Сравнение:** Скрипт находит, что случилось **позже**:
* Если **Последний Пивот** был позже Сигнала: Якорем считается бар пивота.
* Если **Сигнал** был позже Последнего Пивота: Якорем считается бар сигнала.


3. **Постановка стопа:** Трейлинг-стоп ставится на `low` свечи, **предшествующей якорю** (Якорь - 1).
4. **Трейлинг:** Стоп двигается **только вверх** вслед за `low[1]` каждой новой свечи. Сделка закрывается при касании ценой этого уровня.

---

### 5. ЛОГИКА ВЫХОДА (ШОРТ) — Зеркально

* **Бар Сигнала:** Позднейшее выполнение условий блоков SS1–SS16 (± окно).
* **Стоп:** Сравниваем время Сигнала и время Пивота. Берем то, что было позже (Якорь).
* **Постановка:** Стоп ставится на `high` свечи, **предшествующей якорю** (Якорь - 1).
* **Трейлинг:** Стоп двигается **только вниз** вслед за `high[1]`. Закрытие по стопу.

---

### 6. ДОПОЛНИТЕЛЬНЫЕ ФИЛЬТРЫ И ВИЗУАЛ

* **Блокировщики (LBB / SBB):**
* Запрет входа, если с момента последнего выхода прошло менее N свечей.
* Запрет входа, если в течение последних N свечей индикаторы (RSI/VFI) заходили в экстремально «плохие» зоны (например, RSI > 80 для лонга).


* **Таблица и Метки:**
* Таблица в углу показывает текущие значения RSI, VFI, DI+, тип позиции и активные стопы.
* Метка закрытия: При выходе на график ставится метка с указанием причины (код блока), цены входа/выхода и итогового **P/L %**.


* **Интерфейс:** Все 64 блока настроек сгруппированы через `inline`, чтобы параметры одного условия (чекбокс, знак, значение) стояли в одну строку.

Это описание покрывает 100% функционала твоего ТЗ. Теперь можно переходить к генерации кода по этой спецификации. Запускаем?






























Отлично. Вот полная и финальная сводка всех изменений, исправлений и логических узлов, которые будут реализованы в коде. Проверь каждый пункт:

### 1. Визуал (UI настроек)

* **Сжатие строк:** Я уберу дублирующиеся текстовые подписи внутри блоков (сделаю `title=""` для внутренних полей). Настройки вида `[x] LB1: RSI+VFI | [<] | [30] | [>] | [10]` выстроятся в одну компактную линию.

### 2. Исправление критического бага (Поиск пивотов при входе)

* **Было:** Скрипт искал пивоты, отталкиваясь от бара *предыдущего* входа (переменная `lEntryBar`), что ломало окно поиска и приводило к сделкам в случайных местах.
* **Станет:** Скрипт будет отсчитывать окно поиска пивотов (назад и вперед) **строго от текущей сигнальной свечи** (зеленой для лонга, красной для шорта).

### 3. Логика ВХОДА (Лонг/Шорт)

* **Свечной паттерн:** Ищем сигнал (выполнение условий из блоков LB/SB) на *любой* свече внутри паттерна (от первой из 3+ красных до закрытия 1 зеленой).
* **Без пивотов:** Если пивоты выключены, входим сразу по закрытию зеленой (для лонга) или красной (для шорта) свечи.
* **С пивотами:** Точка отсчета — сигнальная свеча. Если нужный паттерн пивотов (с игнорированием шума) уже сформировался в прошлом (в окне "назад") — входим сразу. Если второй пивот формируется в "окне вперед" — ждем его и входим по закрытию свечи этого последнего пивота.

### 4. Прямое закрытие (LSD/SSD - Без фильтров)

* **Отмена окна:** Я полностью убираю окно поиска (lookback) для прямой продажи.
* **Логика:** Скрипт смотрит только на текущую свечу. Как только условие (например, RSI > 80) выполнилось — он моментально активирует трейлинг-стоп, ставя его под минимум (для лонга) или максимум (для шорта) *предыдущей* свечи.

### 5. Основное закрытие (Каскад LS/SS)

* **Каскадный поиск:** Если в блоке 2 или 3 условия, скрипт находит первое, а остальные ищет в радиусе `± окно` от него. Порядок возникновения условий на графике не важен.
* **Сигнальный бар:** Из всех найденных условий скрипт выбирает то, которое произошло *позже всего по времени*. Этот бар становится "баром сигнала на выход".

### 6. Пивоты при выходе и постановка Стопа

* **Поиск "без шума":** Скрипт ищет первый целевой пивот. Найдя его, он ищет второй строго *после* первого. Любые другие пивоты, возникшие между ними, игнорируются (например, в ряду `HH -> HL -> LL -> LH` скрипт спокойно найдет пару `HH -> LH`).
* **Сравнение времени:** Если пивот-фильтр включен, скрипт дожидается окончания "окна вперед" и сравнивает: что было позже — *Бар сигнала на выход* или *Бар последнего найденного пивота*?
* **Постановка стопа:** Стоп ставится на минимум (для лонга) или максимум (для шорта) свечи, **ПРЕДШЕСТВУЮЩЕЙ тому бару, который оказался позже**.
* **Трейлинг:** После постановки стоп двигается *только* в сторону профита (вверх для лонга, вниз для шорта) по экстремумам предыдущих свечей. Закрытие происходит, когда цена пробивает этот стоп.

### 7. Общая механика

* **Pyramiding = 2:** Лонг и Шорт работают полностью независимо, каждый ведет свой собственный трейлинг-стоп.
* **Кнопка "Переворот":** Если включена — при возникновении сигнала в противоположную сторону текущая позиция закрывается ("Flip"). Если выключена — сделки наслаиваются.

Всё собрано точно по твоим инструкциям. Ничего не упустил? Если подтверждаешь — я прямо сейчас генерирую финальный код.




















## ФИНАЛЬНАЯ ТАБЛИЦА:

---

### 🔴 УБИРАЕМ ИЗ НАСТРОЕК:

| Параметр | Значение |
|---|---|
| Период RSI | 14 |
| Длина VFI | 130 |
| Коэффициент VFI | 0.2 |
| Макс. объём VFI | 2.5 |

---

### 🟢 НОВЫЕ ПАРАМЕТРЫ:

| Параметр | По умолчанию |
|---|---|
| Окно поиска сигнала при выходе из Лонга | 5 |
| Окно поиска сигнала при выходе из Шорта | 5 |

---

### 🔵 ВХОД В ЛОНГ:

```
Паттерн: 3+ красных → 1 зелёная

На ВСЕХ свечах паттерна ищем сигнал:

  Блок с N условиями (RSI + VFI + ADX и т.д.):
    Ищем каждое условие независимо
    на любой свече паттерна →
    Все N найдены → блок сработал

  Блок с 1 условием:
    Нашли на любой свече → сработал

Первый сработавший блок →
Открываем лонг по цене закрытия зелёной свечи
```

---

### 🔵 ВХОД В ШОРТ:

```
Зеркально: 3+ зелёных → 1 красная
Открываем шорт по цене закрытия красной свечи
```

---

### 🔵 ВЫХОД ИЗ ЛОНГА:

```
Ищем первый сработавший блок продажи:

  Блок с N условиями:
    Нашли первое условие на любом баре →
    От этого бара ±signalWindowLong ищем второе →
    От второго бара ±signalWindowLong ищем третье →
    И так пока все N не найдены →
    Запомнили бар последнего условия

  Блок с 1 условием:
    Нашли → запомнили этот бар

Если пивот ВЫКЛЮЧЕН:
  Стоп на минимум предыдущей свечи
  перед баром последнего условия

Если пивот ВКЛЮЧЁН:
  Ищем все включённые варианты паттерна
  в окне от бара последнего условия:

    Между первым и вторым пивотом
    могут быть любые другие → игнорируем

    Все варианты найдены →
      Берём бар самого последнего пивота
      из всех найденных вариантов →

      Если этот бар ДО бара сигнала:
        Стоп на минимум предыдущей свечи
        перед баром сигнала

      Если этот бар ПОСЛЕ бара сигнала:
        Стоп на минимум предыдущей свечи
        перед баром этого пивота

    Хоть один вариант не найден → отмена

Стоп двигается вверх за минимум предыдущей свечи
Закрытие по цене стопа
```

---

### 🔵 ВЫХОД ИЗ ШОРТА:

```
Зеркально:

  Если последний пивот ДО бара сигнала:
    Стоп на максимум предыдущей свечи
    перед баром ��игнала

  Если последний пивот ПОСЛЕ бара сигнала:
    Стоп на максимум предыдущей свечи
    перед баром этого пивота

Стоп двигается вниз за максимум предыдущей свечи
Закрытие по цене стопа
```

---

### 🔵 ПИВОТ-ФИЛЬТР:

```
4 фильтра × 4 варианта = 16 строк:
  Вход Лонг:  4 варианта
  Выход Лонг: 4 варианта
  Вход Шорт:  4 варианта
  Выход Шорт: 4 варианта

Каждый вариант включается отдельно
Включено несколько → ВСЕ должны найтись
Между первым и вторым пивотом
что угодно → игнорируем

Если последний пивот ДО бара сигнала:
  Стоп на минимум/максимум предыдущей свечи
  перед баром сигнала

Если последний пивот ПОСЛЕ бара сигнала:
  Стоп на минимум/максимум предыдущей свечи
  перед баром последнего пивота

Хоть один вариант не найден за окно → отмена
```

---

### 🔵 ЛОНГ + ШОРТ ОДНОВРЕМЕННО:

```
pyramiding=2
Независимы, каждый со своим стопом
```

---

### 🔵 КНОПКА ПЕРЕВОРОТ:

```
ВКЛ → при сигнале шорта если открыт лонг:
  сначала закрыть лонг → потом открыть шорт
ВЫКЛ → лонг и шорт живут независимо
```

---

### 🔵 P/L:

```
Считаем до сброса переменных
Метка закрытия: цена входа, цена выхода, P/L%
```

---

### 🔵 ВИЗУАЛ:

```
Компактный inline= везде где возможно
```

---

### ⚪ ОСТАВЛЯЕМ БЕЗ ИЗМЕНЕНИЙ:

```
Блокировщики lbb/sbb
Таблица минимальная
Названия блоков LB/LS/SB/SS
Подсчёт красных/зелёных свечей
Отображение стопа на графике
```

---

Всё верно? Начинаем код?
